@model UrnaElectronica.Models.Eleccion
@{ Layout = null; }

<div id="modal-backdrop" style="position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.6); display: flex; align-items: center; justify-content: center; z-index: 9999;">
    <div style="background: white; padding: 2rem; border-radius: 12px; width: 500px; max-width: 95%; box-shadow: 0 10px 25px rgba(0,0,0,0.3);">
       <div class="modal-header">
            <h3 style="margin-top: 0; color: #333;">Editar Elección</h3>
            <hr />
         </div>
        <form hx-post="/Eleccion/Edit" hx-target="#modal-container">
            @Html.AntiForgeryToken()
            <input type="hidden" name="Id" value="@Model.Id" />
            @* Es importante mantener el IdProceso para que la entidad siga ligada al proceso correcto *@
            <input type="hidden" name="IdProceso" value="@Model.IdProceso" />

            <div style="margin-bottom: 1rem;">
                <label class="form-label">Nombre de la Elección:</label>
                <input type="text" name="Nombre" value="@Model.Nombre" class="form-control" required>
            </div>

            <div class="form-status-container">
                <label class="form-check-label">
                    <input type="checkbox" asp-for="Activo" />
                    <span>¿Elección actualmente activa?</span>
                </label>
                <p class="form-help-text">Si se desmarca, la eleccion no aparecerá en las boletas electorales.</p>
            </div>

            <div style="text-align: right; padding-top: 1rem; border-top: 1px solid #eee;">
                <button type="button" onclick="document.getElementById('modal-backdrop').remove()" 
                        class="btn-cancel-modal">Cancelar</button>
                <button type="submit" class="btn-save-modal">Guardar Cambios</button>
            </div>
        </form>
    </div>
</div>